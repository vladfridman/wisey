package wisey.io;

import wisey.lang.IException;

/**
 * Exception that is thrown by wisey.io.CFile when trying to get a reference to the __sFILE struct
 * presumably for a write or read operation and without the file being opened
 */
model MFileAccessException implements IException {

  public void report() override {
    callstack.reportException(this);
    printerr("Details: Trying to operate a file that is not opened\n"); 
  }
}