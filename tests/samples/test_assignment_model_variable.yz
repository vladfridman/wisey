package systems.vos.wisey.compiler.tests;

model MRectangle {
  int mWidth;
  int mHeight;
}

controller CProgram implements IProgram {

  public int run() {
    MRectangle r1 = builder(MRectangle)
      .withWidth(1)
      .withHeight(3)
      .build();

    MRectangle r2 = r1;

    return 0;
  }
}

bind(CProgram).to(IProgram);
