package systems.vos.wisey.compiler.tests;

import wisey.io.CTextStreamReader;
import wisey.lang.NString;
import wisey.lang.IProgram;
import wisey.lang.MString;

controller CProgram implements IProgram {

  public int run(immutable string[] arguments) override {
    CTextStreamReader* stream = CTextStreamReader.open(
      NString.createFromString("tests/samples/inputs/textfile.txt") -> NString*) -> CTextStreamReader*;
    NString* line = stream:wisey.io.CTextStreamReader*:readLine() -> NString*;
    while (line != null) {
      printout(line + "\n");
      line = stream:wisey.io.CTextStreamReader*:readLine() -> NString*;

    }
    stream:wisey.io.CTextStreamReader*:close();
    return 1;
  }
}

bind(IProgram, CProgram);
