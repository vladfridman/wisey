model MModel {
  public int foo(int a, int b, int c) {
    return a + b + c;
  }
}

controller CProgram implements IProgram {

  public int run() {
    MModel myModel = builder(MModel).build();
    return myModel.foo(1, 2, 3);
  }
}

bind(CProgram).to(IProgram);
