package systems.vos.wisey.compiler.tests;

import wisey.lang.CString;
import wisey.lang.IProgram;
import wisey.lang.MString;

controller CProgram implements IProgram {

  public int run(immutable string[] arguments) override {
    CString* cstring = CString.create("123");
    MString* mstring = cstring.toModel();
    printout("before: " + cstring + " length: " + cstring.getLength() + " capacity: " + cstring.getCapacity() + "\n");
    printout("after: " + mstring + " length: " + mstring.getLength() + "\n");
    return (int) mstring.getLength();
  }
}

bind(IProgram).to(CProgram);
