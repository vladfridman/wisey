package systems.vos.wisey.compiler.tests;

import wisey.lang.CString;
import wisey.lang.IProgram;

controller CProgram implements IProgram {

  public int run(immutable string[] arguments) override {
    CString* cstring = CString.createFromString("123456");
    printout("Capacity before: " + cstring.getCapacity() + "\n");
    cstring.appendString("7");
    printout(cstring + "\n");
    printout("Capacity after: " + cstring.getCapacity() + "\n");
    return (int) cstring.getLength();
  }
}

bind(IProgram).to(CProgram);
