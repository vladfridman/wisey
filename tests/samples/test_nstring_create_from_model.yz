package systems.vos.wisey.compiler.tests;

import wisey.lang.NString;
import wisey.lang.IProgram;
import wisey.lang.MString;

controller CProgram implements IProgram {

  public int run(immutable string[] arguments) override {
    MString* mstring = MString.createFromString("123") -> MString*;
    NString* nstring = NString.createFromModel(mstring) -> NString*;
    printout("mstring: " + mstring + " length: " + mstring:wisey.lang.MString*.getLength() -> long + "\n");
    printout("nstring: " + nstring + " length: " + nstring:wisey.lang.NString*.getLength() -> long + " capacity: " + nstring:wisey.lang.NString*.getCapacity() -> long + "\n");
    return (int) mstring:wisey.lang.MString*.getLength() -> int;
  }
}

bind(IProgram, CProgram);
