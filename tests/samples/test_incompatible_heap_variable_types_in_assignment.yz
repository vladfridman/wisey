package systems.vos.wisey.compiler.tests;

model MColor {
  int mValue;
}

model MShape {
  int mWidth;
  int mHeight;
}

controller CProgram implements IProgram {

  public int run() {
    MColor color = builder(MShape).withWidth(1).withHeight(2).build();
    return 0;
  }
} 

bind(CProgram).to(IProgram);
