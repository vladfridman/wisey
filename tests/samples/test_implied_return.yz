model MModel {
  public void foo() {
    int i = 0;
  }
}

controller CProgram implements IProgram {

  public int run() {
    MModel m = builder(MModel).build();
    m.foo();

    return 5;
  }
} 

bind(CProgram).to(IProgram);
