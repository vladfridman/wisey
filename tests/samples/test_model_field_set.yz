model MColor {
  int mValue;

  public void setValue(int a) {
    mValue = a;
  }

  public int getValue() {
    return mValue;
  }
}

public int main() {
  MColor color = builder(MColor).withValue(5).build();
  
  color.setValue(7);

  return color.getValue();
}
