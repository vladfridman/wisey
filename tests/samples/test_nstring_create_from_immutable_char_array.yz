package systems.vos.wisey.compiler.tests;

import wisey.lang.NString;
import wisey.lang.IProgram;
import wisey.lang.MString;

controller CProgram implements IProgram {

  public int run(immutable string[] arguments) override {
    char[]* array = new char[5];
    array[0] = 'd';
    array[1] = 'e';
    array[2] = 'f';
    immutable char[]* immutableArray = array;
    NString* nstring = NString.createFromImmutableCharArray(immutableArray) -> NString*;
    printout("nstring: " + nstring + " length: " + nstring.getLength() -> long + " capacity: " + nstring.getCapacity() -> long + "\n");
    return (int) nstring.getLength() -> int;
  }
}

bind(IProgram, CProgram);
