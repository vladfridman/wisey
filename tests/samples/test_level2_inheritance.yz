package systems.vos.wisey.compiler.tests;

import wisey.lang.IProgram;

interface IObject {
  int getColor(int a);
}

interface IShape extends IObject {
  int getPerimeter();
  int getArea();
}

interface IClickable {
  void click();
  void tap();
}

model MSquare implements IClickable, IShape {
  int mWidth;

  public int getPerimeter() override {
    return mWidth + mWidth;
  }

  public int getArea() override {
    return mWidth * mWidth;
  }

  public int getColor(int a) override {
    return a;
  }

  public void click() override {
  }

  public void tap() override {
  }
}

controller CProgram implements IProgram {

  public int run(immutable string[] arguments) override {
    MSquare* square = build(MSquare).withWidth(5).onHeap() -> MSquare*;

    return square:systems.vos.wisey.compiler.tests.MSquare*:getPerimeter() -> int + square:systems.vos.wisey.compiler.tests.MSquare*:getArea() -> int + square:systems.vos.wisey.compiler.tests.MSquare*:getColor(55) -> int +
      ((IShape) square):IShape:getPerimeter() -> int + ((IShape) square):IShape:getArea() -> int +
      ((IShape) square):IShape:getColor(55) -> int + ((IObject) square):IObject:getColor(55) -> int;
  }
}

bind(IProgram, CProgram);
