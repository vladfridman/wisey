model MColor {
  int mValue;

  public int getValue(float a, int b) {
    return mValue + b;
  }
}

int main() {
  MColor color = builder(MColor).withValue(5).build();

  return color.getValue(2.4, 3);
}
