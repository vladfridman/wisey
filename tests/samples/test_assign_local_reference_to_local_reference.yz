package systems.vos.wisey.compiler.tests;

model MCar {
  int mValue;
}

model MRunner {
  public static void run() {
    MCar reference = builder(MCar).withValue(5).build();
    MCar anotherReference = reference;
  }
}
