package systems.vos.wisey.compiler.tests;

node NNode {
  int mValue;

  public int getValue() {
    return mValue;
  }
}

controller CProgram implements IProgram {

  public int run() {
    NNode* color = builder(NNode).withValue(5).build();

    return color.getValue();
  }
}

bind(CProgram).to(IProgram);
