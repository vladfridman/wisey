package systems.vos.wisey.compiler.tests;

import wisey.lang.CString;
import wisey.lang.IProgram;
import wisey.lang.MString;

controller CProgram implements IProgram {

  public long run(immutable string[] arguments) override {
    char[]* array = new char[5];
    array[0] = 'd';
    array[1] = 'e';
    array[2] = 'f';
    immutable char[]* immutableArray = array;
    CString* cstring = CString.createFromImmutableCharArray(immutableArray);
    printout("cstring: " + cstring + " length: " + cstring.getLength() + " capacity: " + cstring.getCapacity() + "\n");
    return (int) cstring.getLength();
  }
}

bind(IProgram).to(CProgram);
