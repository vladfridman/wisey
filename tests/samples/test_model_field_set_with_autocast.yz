model MColor {
  int mValue;

  public void setValue(boolean a) {
    mValue = a;
  }

  public int getValue() {
    return mValue;
  }
}

int main() {
  MColor color = builder(MColor).withValue(5).build();
  
  color.setValue(true);

  return color.getValue();
}
