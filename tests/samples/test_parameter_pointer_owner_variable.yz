package systems.vos.wisey.compiler.tests;

import wisey.lang.IProgram;

model MModel {
  int mValue;
}

controller CProgram implements IProgram {

  public void free(::llvm::i32::pointer* pointer) {

  }

  public int run() {
    MModel* mymodel = builder(MModel).withValue(5).build();
    free(mymodel);
    printout("free completed\n");

    return 1;
  }
}

bind(CProgram).to(IProgram);
