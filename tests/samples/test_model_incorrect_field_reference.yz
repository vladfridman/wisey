package systems.vos.wisey.compiler.tests;

model MColor {
  int mIntensity;
}

controller CProgram implements IProgram {

  public int run() {
    MColor color = builder(MColor).withIntensity(2).build();

    return color.mWidth;
  }
}

bind(CProgram).to(IProgram);
