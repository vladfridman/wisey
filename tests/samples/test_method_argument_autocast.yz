model MColor {
  int value;

  public void setValue(int val) {
    value = val;
  }

  public int getValue() {
    return value;
  }
}

public int main() {
  MColor color = builder(MColor).withValue(2).build();

  color.setValue(true);

  return color.getValue();
}