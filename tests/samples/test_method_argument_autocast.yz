model MColor {
  int mValue;

  public void setValue(int val) {
    mValue = val;
  }

  public int getValue() {
    return mValue;
  }
}

public int main() {
  MColor color = builder(MColor).withValue(2).build();

  color.setValue(true);

  return color.getValue();
}