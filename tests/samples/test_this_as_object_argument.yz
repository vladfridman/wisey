package systems.vos.wisey.compiler.tests;

import wisey.lang.IProgram;

model MCaller {
  public int makeCall(CProgram object) {
    return object:systems.vos.wisey.compiler.tests.CProgram->getValue() ~> int;
  }
}

controller CProgram implements IProgram {

  public int run(immutable string[] arguments) override {
    MCaller* caller = build(MCaller).onHeap() ~> MCaller*;

    return caller:systems.vos.wisey.compiler.tests.MCaller*->makeCall(this) ~> int;
  }

  public int getValue() {
    return 3;
  }
}

bind(IProgram, CProgram);
