package systems.vos.wisey.compiler.tests;

import wisey.lang.IProgram;

model MData {
  int mValue;

  public int getValue() {
    return mValue;
  }
}

controller CService {
  state MData* mData;

  public void set() {
    mData = build(MData).withValue(1).onHeap() -> MData*;
  }

  public void clear() {
    mData = null;
  }

  public int getValue() {
    return mData:systems.vos.wisey.compiler.tests.MData*:getValue() -> int;
  }
}

controller CProgram implements IProgram {
  inject CService* mService;

  public int run(immutable string[] arguments) override {
    mService:systems.vos.wisey.compiler.tests.CService*:set();
    mService:systems.vos.wisey.compiler.tests.CService*:clear();

    return mService:systems.vos.wisey.compiler.tests.CService*:getValue() -> int;
  }
} 

bind(IProgram, CProgram);
