package systems.vos.wisey.compiler.tests;

node NCar {
  int mYear;

  public int getYear() {
    return mYear;
  }
}

controller CProgram implements IProgram {

  public int run() {
    NCar* car = builder(NCar).withYear(false).build();
    
    return car.getYear();
  }
}

bind(CProgram).to(IProgram);
